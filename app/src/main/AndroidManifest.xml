<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.annaalfiani.gmcapps">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.select_seat.SeatActivity"></activity>
        <activity android:name=".TiketActivity" />
        <activity
            android:name=".DetailMovieActivity"
            android:label="@string/title_activity_detail_movie"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity android:name=".ui.detail_movie.DetailActivity" />
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".Home.HomeActivity" />
        <activity android:name=".Onboarding.OnboardingOneActivity" />
        <activity android:name=".Onboarding.Onbording_TwoActivity" />
        <activity android:name=".Onboarding.Onbording_TreeActivity" />
        <activity android:name=".ui.login.SignInActivity" />
        <activity android:name=".ui.register.SignUpActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" />
    </application>

</manifest>